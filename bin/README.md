# Prebuilt binaries

The release CI produces:

- `stratum_v2_probe-linux-x86_64-musl`
- `stratum_v2_probe-linux-x86_64-musl.sha256`

After a successful verified CI build, copy the artifact files here and commit them if this repository ships prebuilt binaries.

Do not place an unverified placeholder executable here.
